import { Component } from '@angular/core';
// import {Hero} from './hero';


@Component({
	moduleId: module.id,
    selector: 'my-app',
    templateUrl: 'app.component.html'
})
export class AppComponent { 
	// title = "Test Title ";
	// heros = [new Hero(1, "Ken"), new Hero(2, "Kan"), new Hero(3, "Kenny"), new Hero(4, "Alex")];
	// myHero = this.heros[0];

	// addHero(newHero: string) {
	//     if (newHero) {
	//       this.heros.push(new Hero(this.heros.length, newHero));
	//     }
	//   }
}
